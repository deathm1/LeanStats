import React from "react";
import ReactDOM from "react-dom/client";
import MasterInterface from "./MasterInterface/MasterInterface";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MasterInterface />
  </React.StrictMode>
);
